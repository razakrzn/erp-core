$kind: http-request
method: GET
url: '{{baseUrl}}/api/v1/users/'
order: 4000
headers:
  - key: Accept
    value: application/json
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{AccessToken}}'
examples: ./.resources/users - list.resources/examples
