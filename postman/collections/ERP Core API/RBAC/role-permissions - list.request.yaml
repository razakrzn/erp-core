$kind: http-request
method: GET
url: '{{baseUrl}}/api/v1/role-permissions/'
order: 28000
headers:
  - key: Accept
    value: application/json
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{AccessToken}}'
