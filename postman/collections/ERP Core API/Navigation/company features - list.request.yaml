$kind: http-request
method: GET
url: '{{baseUrl}}/api/v1/navigation/company/features/'
order: 11000
headers:
  - key: Accept
    value: application/json
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{AccessToken}}'
