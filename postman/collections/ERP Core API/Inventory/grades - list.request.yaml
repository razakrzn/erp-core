$kind: http-request
method: GET
url: '{{baseUrl}}/api/v1/inventory/grades/'
order: 52000
headers:
  - key: Accept
    value: application/json
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{AccessToken}}'
