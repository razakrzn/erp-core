$kind: http-request
method: GET
url: '{{baseUrl}}/api/v1/inventory/materials/'
order: 43000
headers:
  - key: Accept
    value: application/json
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{AccessToken}}'
