#!/usr/bin/env bash
set -euo pipefail

# Git post-merge hook: regenerate OpenAPI + Postman artifacts after `git pull`.
# Safe + idempotent: overwrites generated files only.

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)"

"$ROOT_DIR/scripts/postman-sync.sh" || true
